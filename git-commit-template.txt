(RED|YELLOW|GREEN|REFACTOR|DEV|DOC): <Imperative summary (initial cap, no period)
# RED - introduces test failures
# YELLOW - incremental work towards green
# GREEN - resolves all test failures
# REFACTOR - reorganizes without affecting test results
# DEV - only updates dev tooling
# DOC - only updates documentation

# Use simple markdown for the following sections (no headers (conflicts with comments))

**What this does**
(if more elaboration desired after the summary, otherwise, delete)

**Why this is being done**
(this is required)
